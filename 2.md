# TVBox 接口备份自动化 — 项目完整说明

> 🌟 **第一次接触？请先阅读 [../QUICKSTART.md](../QUICKSTART.md)（新手 5 分钟上手指南）**，再回来看本文档的细节。

## 目录

1. [项目总览](#1-项目总览)
2. [仓库目录结构](#2-仓库目录结构)
3. [脚本 A：rydownload.py](#3-脚本-a-rydownloadpy)
4. [脚本 B：apidownload.py](#4-脚本-b-apidownloadpy)
5. [脚本 C：livedownload.py](#5-脚本-c-livedownloadpy)
6. [本地手动运行指南](#6-本地手动运行指南)
7. [GitHub Actions 工作流部署](#7-github-actions-工作流部署)
8. [三工作流协作与避坑](#8-三工作流协作与避坑)
9. [配置修改速查](#9-配置修改速查)
10. [故障排查](#10-故障排查)
11. [一键校验清单](#11-一键校验清单)
12. [网站子系统（index.html）](#12-网站子系统indexhtml) ⭐ 新增

---

## 1. 项目总览

```
┌────────────────────────────────────────────────────────────┐
│              GitHub: lubin776/tvbox-api-backup (main)      │
│                                                            │
│  rylinks.txt ──▶ python/rydownload.py  ──▶ ry/            │ ──▶ git push
│       (每日, 强制覆盖)                                      │
│                                                            │
│ tvboxapilinks.txt │ python/apidownload.py ──▶ tvbox/       │ ──▶ git push
│     (纯文本)      │    (每两日, 奇偶日 gate)                │
│                    │    生成 list.txt                       │
│                                                            │
│ tvbox/*.json ──▶ python/livedownload.py ──▶ tvbox/live/   │ ──▶ git push
│   (接口文件)     │    (每日, 套壳穿透) 生成 livelist.txt   │
└────────────────────────────────────────────────────────────┘
                        ▲              ▲              ▲
                        └── 共享锁 ────┴── tvbox-repo-write ──┘
                                    │
                                    ▼ 产物即数据源
┌────────────────────────────────────────────────────────────┐
│              🌐 网站子系统（静态单页）                      │
│                                                            │
│   index.html ──读取──▶ list.txt（点播）                    │
│              ──读取──▶ livelist.txt（直播）                │
│              ──读取──▶ config.json → ry/（下载）           │
│                                                            │
│   四个面板：点播 / 直播 / 下载 / 关于                      │
│   特性：搜索、模糊匹配置顶、一键复制、简洁/全能双模式       │
└────────────────────────────────────────────────────────────┘
```

**设计要点：**

| 维度 | 说明 |
|------|------|
| 配置外置 | 所有「名称+URL」从脚本移到仓库根配置文件，改链接不动代码 |
| 脚本统一目录 | 三个脚本均在 `python/` 下，靠 CWD = 仓库根定位配置和产物 |
| 产物隔离 | `ry/` 、 `tvbox/` 、 `tvbox/live/` 分属不同工作流，互不覆盖 |
| 共享锁 | 三个工作流共用 `concurrency: tvbox-repo-write`，串行 push 防冲突 |
| 工作流命名 | 与脚本名一致，仅后缀 `.py` → `.yml` |
| **网站数据驱动** | **网站不写死任何接口，全部从脚本产物 + `config.json` 动态渲染** |

---

## 2. 仓库目录结构

```
tvbox-api-backup/
├── index.html                          ⭐🌐 网站页面（静态单页，四个面板）
├── config.json                         ⭐🌐 网站下载面板数据源配置
├── .github/
│   └── workflows/
│       ├── rydownload.yml              ⭐ 工作流A：每日解析 ry
│       ├── apidownload.yml             ⭐ 工作流B：每两日抓取 tvbox 接口
│       └── livedownload.yml            ⭐ 工作流C：每日聚合直播源
│
├── python/                              ⭐ 脚本统一目录
│   ├── rydownload.py                   ⭐ 脚本A
│   ├── apidownload.py                  ⭐ 脚本B（v2：支持纯文本配置）
│   └── livedownload.py                 ⭐ 脚本C
│
├── rylinks.txt                          ⭐ 脚本A 配置（名称+URL，纯文本）
├── tvboxapilinks.txt                    ⭐ 脚本B 配置（名称+URL，纯文本，v2）
├── site_check.py                        🌐 网站完整性校验脚本
│
├── ry/                                  ← 脚本A 产物（自动生成）
│   └── single.json                      ← 🌐 下载资源清单（网站下载面板读取）
├── tvbox/                               ← 脚本B 产物（自动生成）
│   ├── *.json                           ← 各线路接口文件
│   ├── 海量直播线路.json                 ← 脚本C 聚合锚点（自动生成）
│   └── live/                            ← 脚本C 播放列表输出（自动生成）
│       ├── xxx.m3u
│       └── xxx.txt
├── livelist.txt                         ← 脚本C 产物（直播索引）← 网站直播面板数据源
├── list.txt                             ← 脚本B 产物（接口清单）← 网站点播面板数据源
├── SUMMARY.txt                          ← 脚本B 产物（运行摘要）
│
└── README.md
```

---

## 3. 脚本 A：rydownload.py

### 3.1 作用

批量下载「本地包/下载资源」类接口（如菠菜园下载、潇洒下载等），解析 JSON，把结果落到 `ry/` 目录。每次运行**强制覆盖**旧产物。

### 3.2 配置：`rylinks.txt`（仓库根，文本格式）

```
潇洒下载, https://9877.kstore.space/single.json
奇奇下载, http://bd.qiqiv.cn/666.json
菠菜园下载, https://0.12yue.de5.net/tvbox/x/lib/菠菜园下载.json
柒豪下载, https://raw.gitcode.com/qihao/qihaoyyds/raw/main/版本.json
```

**语法规则：**
- 每行一条，格式 `名称, URL`（英文逗号分隔，两边可有空格）
- `#` 开头 = 注释
- 名称可省略，脚本自动取域名作文件名
- 支持 `file://`（本地）和 `raw:base64`（内联）用于调试

### 3.3 运行流程

```
读取 rylinks.txt → 逐行解析 → HTTP 请求（UA 轮转、重试 3 次）
→ 解析 JSON → 写出到 ry/（强制覆盖）
```

### 3.4 运行方式

```bash
python3 python/rydownload.py
python3 python/rydownload.py -c /path/to/other.txt   # 指定配置
python3 python/rydownload.py -l "URL" --name 测试      # 单链接调试
```

---

## 4. 脚本 B：apidownload.py

### 4.1 作用

抓取 TVBox 的「接口线路」（`饭太硬`、`嗷呜`、`肥猫` 等），**一行可配多镜像**，自动选通，生成 `tvbox/*.json` + `list.txt` + `SUMMARY.txt`。

> ✅ **v2 配置简化**：`tvboxapilinks.txt` 已改为**纯文本格式**（和 `rylinks.txt` 一样），不再需要 JSON 语法。

### 4.2 配置：`tvboxapilinks.txt`（仓库根，纯文本格式 · 推荐）

```txt
# 格式：名称, 主URL, 镜像URL1, 镜像URL2
饭太硬, http://www.饭太硬.net/tv, http://www.饭太硬.cc/tv
嗷呜, http://www.英格里希嗷呜.top/tv
肥猫, https://example.com/fatcat.json

# 省略名称，自动用域名命名
https://no-name.com/api.json
```

**语法规则：**
- 每行一条，格式 `名称, URL1, URL2, ...`（英文逗号分隔，两边可有空格）
- **第一个 URL 为主接口，后续自动作为镜像**
- `#` 开头 = 注释；空行忽略
- 名称可省略，脚本自动取域名作接口名

> 💡 加接口 = 加一行，完事。多镜像 = 同一行多写几个逗号隔开的 URL。

### 4.3 运行流程

```
读取 tvboxapilinks.txt（纯文本）
│ → 逐行解析：名称 + [主URL, 镜像...]
│ → 对每个接口依次尝试 URL，首个成功即下载
│ → 自动解密（2423-AES / base64 / gzip / **壳）
│ → 写出 tvbox/{名称}.json
│ → 生成 list.txt + SUMMARY.txt
```

### 4.4 运行方式

```bash
python3 python/apidownload.py
python3 python/apidownload.py --check-config   # 只校验配置，不联网（推荐先跑）
python3 python/apidownload.py --debug
```

---

## 5. 脚本 C：livedownload.py

### 5.1 作用

从 TVBox 接口 JSON（`tvbox/*.json`）的 `lives` 数组中批量提取直播源，模拟 TVBox 客户端环境下载播放列表（自动穿透套壳），生成 `tvbox/live/` 下的播放列表文件，维护 `livelist.txt` 索引。

**核心原则**：对外文件名锚定原始条目（不受套壳跳转影响），保证外部访问路径长期稳定。

### 5.2 运行流程

1. **扫描提取**：遍历 `tvbox/*.json` → 读取 `lives` → 提取有 name 且有 url 的条目（URL 去重）
2. **聚合命名**：名称冲突自动编号 `xxx → xxx2线 → xxx3线`，写入 `tvbox/海量直播线路.json` 锚点
3. **批量下载**：模拟 TVBox 请求，套壳检测（最多递归 5 层），写出播放列表
4. **生成索引**：更新 `livelist.txt`（旧记录保留，成功记录覆盖）

### 5.3 运行方式

```bash
python3 python/livedownload.py
python3 python/livedownload.py --debug
python3 python/livedownload.py --force
```

---

## 6. 本地手动运行指南

```bash
# Step 1: 克隆并进入仓库根
git clone https://github.com/lubin776/tvbox-api-backup.git
cd tvbox-api-backup

# Step 2: 安装依赖
pip3 install -r requirements.txt

# Step 3-5: 依次运行三个脚本
python3 python/rydownload.py --debug
python3 python/apidownload.py --debug
python3 python/livedownload.py --debug

# Step 6: 验证并提交
ls ry/ tvbox/ tvbox/live/ list.txt SUMMARY.txt livelist.txt
git add . && git commit -m "test: 本地验证" && git push
```

---

## 7. GitHub Actions 工作流部署

三个工作流文件位于 `.github/workflows/`，推送到 `main` 分支后 GitHub 自动识别。

| 工作流 | Cron（UTC） | 北京时间 | 功能 |
|--------|-------------|---------|------|
| `rydownload.yml` | `0 17 * * *` | 每日 01:00 | 每日解析 ry |
| `apidownload.yml` | `5 16 * * *` | 每两日 00:05 | 每两日抓取接口（奇偶日 gate） |
| `livedownload.yml` | `0 18 * * *` | 每日 02:00 | 每日聚合直播源 |

三个工作流共用 `concurrency: tvbox-repo-write`，串行 push 防冲突。

---

## 8. 三工作流协作与避坑

### 8.1 协作矩阵

| 维度 | rydownload | apidownload | livedownload |
|------|-----------|-------------|--------------|
| 触发 | 每日 01:00(北京) | 每两日 00:05(北京) | 每日 02:00(北京) |
| 配置 | `rylinks.txt` | `tvboxapilinks.txt` | `tvbox/*.json`（输入） |
| 产物 | `ry/` | `tvbox/*.json` + `list.txt` + `SUMMARY.txt` | `tvbox/live/` + `livelist.txt` + `海量直播线路.json` |
| git add | `ry/ rylinks.txt` | `tvbox/ list.txt SUMMARY.txt tvboxapilinks.txt` | `tvbox/ livelist.txt` |
| 共享锁 | `tvbox-repo-write` | `tvbox-repo-write` | `tvbox-repo-write` |

### 8.2 避坑清单

| 坑 | 解决 |
|----|------|
| YAML 缩进错误 | `uses`/`run` 与 `name` 同级对齐 |
| 并发 push 冲突 | 共享锁 + `git pull --rebase` |
| 产物互相覆盖 | 严格按协作矩阵 `git add`，绝不用 `git add -A` |
| CWD 不对 | 工作流已设 `working-directory`，本地务必 `cd` 到仓库根 |
| 奇偶日误判 | 偶日定时触发秒过=正常跳过；手动触发不受限 |
| 脚本C与B产物交叉 | livedownload 只写 `tvbox/live/` + `tvbox/海量直播线路.json` + `livelist.txt` |

---

## 9. 配置修改速查

| 想做什么 | 改哪里 | 需重新部署 |
|---------|--------|-----------|
| 增删 ry 下载链接 | 编辑 `rylinks.txt` | ❌ 自动生效 |
| 改 ry 超时/UA | `python/rydownload.py` 常量区 | ✅ push 后 |
| 增删 tvbox 接口 | 编辑 `tvboxapilinks.txt` 的 `API_LIST` | ❌ 自动生效 |
| 调整镜像 | 编辑 `API_MIRRORS` | ❌ 自动生效 |
| 改调度时间 | 对应 `.yml` 的 `cron` | ✅ push 后 |
| 改产物目录 | 脚本 `OUTPUT_DIR` + 工作流 `git add` | ✅ push 后 |

---

## 10. 故障排查

| 现象 | 原因 | 解决 |
|------|------|------|
| 所有条目失败 | 网络不通 / UA 被封 | 加 `--debug`，检查 UA 池 |
| 套壳未展开 | 返回内容 URL > 1 条 | 正常行为，多条视为播放列表本身 |
| 文件名含特殊字符 | 净化规则未覆盖 | 检查 `sanitize_stem()` 正则 |
| livelist 旧记录丢失 | 名称匹配失败 | 旧记录按 stem 匹配，大改名会丢失 |
| Actions 页面看不到工作流 | `.yml` 不在 `.github/workflows/` | 检查路径 |
| `requests` ImportError | 依赖未装 | 检查 `pip install requests` 步骤日志 |

---

## 11. 一键校验清单

```bash
# 在仓库根执行
echo "=== 1. 结构检查 ==="
ls .github/workflows/rydownload.yml .github/workflows/apidownload.yml .github/workflows/livedownload.yml
ls python/rydownload.py python/apidownload.py python/livedownload.py
ls rylinks.txt tvboxapilinks.txt

echo "=== 2. YAML 语法校验 ==="
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/rydownload.yml')); print('ry OK')"
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/apidownload.yml')); print('api OK')"
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/livedownload.yml')); print('live OK')"

echo "=== 3. 配置校验 ==="
python3 python/apidownload.py --check-config

echo "=== 4. 本地试跑 ==="
python3 python/rydownload.py
python3 python/apidownload.py
python3 python/livedownload.py

ls ry/ tvbox/ tvbox/live/ list.txt SUMMARY.txt livelist.txt
```

全部通过 → 部署成功。

---

---

## 12. 网站子系统（index.html）⭐

### 12.1 是什么

`index.html` 是一个**零依赖静态单页**：无需构建、无需后端、无需数据库。它直接读取仓库内的脚本产物（`list.txt` / `livelist.txt` / `config.json` / `ry/`），渲染成可搜索、可一键复制的接口导航站。

**核心原则：数据驱动 —— 网站不写死任何接口，全部由脚本自动产出。**

### 12.2 数据流（关键）

```
脚本B (apidownload.py)  ──▶  list.txt        ──▶  网站「点播」面板
脚本C (livedownload.py) ──▶  livelist.txt    ──▶  网站「直播」面板
脚本A (rydownload.py)   ──▶  ry/single.json  ──▶  config.json ──▶ 网站「下载」面板
```

| 面板 | 数据文件 | 格式 | 渲染内容 |
|------|---------|------|---------|
| 点播 | `list.txt` | `名称|日期|大小|原始URL` | 接口卡片 + 三备份域名线路 + 一键复制 |
| 直播 | `livelist.txt` | `名称|日期|大小|原始URL|来源|UA` | 直播源卡片 + 来源/UA 信息 |
| 下载 | `config.json` → `ry/*.json` | JSON（见下） | 客户端/资源下载卡片（图标、版本） |
| 关于 | 页内固定 | — | 备份域名、免责声明、QQ 群 |

### 12.3 配置文件：config.json

网站**唯一需要维护的配置**是仓库根的 `config.json`，格式：

```json
{
  "_说明": "下载面板数据源。改这里，不用动代码。",
  "downloadSources": [
    {
      "name": "本地包资源",
      "description": "由 rydownload.py 抓取的下载资源",
      "url": "ry/single.json"
    }
  ]
}
```

每个 `downloadSources[].url` 指向的 JSON 需满足渲染结构：

```json
{
  "list": [
    {
      "name": "分组名（如 TVBox 客户端）",
      "list": [
        { "name": "名称", "url": "https://...", "icon": "https://...", "version": "v1.0" }
      ]
    }
  ]
}
```

> 💡 增加下载资源 = 编辑 `config.json` 的 `downloadSources`，或让脚本A自动生成符合结构的 `ry/single.json`。**无需改前端代码。**

### 12.4 四个面板与特性

- **点播**：按 `API_WEIGHTS` 权重排序（饭太硬、肥猫等固定权重），「模糊搜索」类接口随机置顶；每条接口展示「原始线路 + 三备份域名」共 4 个可复制 URL
- **直播**：展示聚合直播源，附带来源 JSON、UA 指纹信息
- **下载**：网格卡片，图标 + 名称 + 版本号，点击外链下载
- **关于**：备份域名一键复制、免责声明、QQ 群
- **全局**：顶部搜索框实时过滤；左上角「切换简洁/全能版」按钮；一键复制带成功反馈

### 12.5 本地预览（重要：CORS）

由于网站用 `fetch()` 读取本地文件，**直接双击 `index.html`（`file://` 协议）会被浏览器 CORS 拦截**，导致面板加载失败。两种解决方式：

**方式一：起本地服务器（推荐，一行命令）**
```bash
# 在仓库根目录执行
python3 -m http.server 8000
# 浏览器打开 http://localhost:8000
```

**方式二：部署到静态托管（GitHub Pages / Vercel / Netlify）**
推送后开启 GitHub Pages 即可获得公网地址，天然无 CORS 问题。

> ⚠️ 必须先把 `list.txt` / `livelist.txt` 生成出来（跑过脚本B/C，或让 CI 先跑一次），否则对应面板显示「列表为空」。

### 12.6 校验网站

```bash
python3 site_check.py
```

校验项：HTML 基本结构、`config.json` 合法性、`downloadSources` 指向文件是否存在、`list.txt`/`livelist.txt` 是否已生成。

完整校验（含网站）：
```bash
bash validate.sh
```

### 12.7 常见定制

| 想做什么 | 改哪里 | 需改代码 |
|---------|--------|---------|
| 改备份域名（三线路） | `index.html` 的 `API_DOMAINS` / `LIVE_DOMAINS` | ❌ 改常量即可 |
| 调接口排序权重 | `index.html` 的 `API_WEIGHTS` | ❌ 改常量即可 |
| 增加下载资源分组 | `config.json` 或 `ry/single.json` | ❌ 纯数据 |
| 改网站标题/Logo/群号 | `index.html` 的 `<h1>` / `.header-logo` / 页脚 | ❌ 改 HTML |
| 加新面板 | 复制一个 `.tab-panel` + 注册到 `tabsData` | ✅ 需懂 JS |

**文档版本**：v3（集成静态网站）  
**项目**：海量接口搬运备份站  
**仓库**：https://github.com/lubin776/tvbox-api-backup
